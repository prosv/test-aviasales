export const reducer = (state = {}, action) => {
    switch (action.type) {
        // case action.type: {
        //     return state;
        // }
        default: {
            return state;
        }
    }
};